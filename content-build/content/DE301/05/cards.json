{
  "cards": [
    {
      "concept": "Deterministic embedding ID",
      "intuition": "Bind vector identity to exact chunk, model revision, and preprocessing semantics.",
      "formula": "embedding_id = hash(chunk_id, model_revision, preprocessing_version)",
      "topic": "Chunk-and-Embed as an Idempotent Batch Job",
      "highway": true
    },
    {
      "concept": "Idempotent embedding retry",
      "intuition": "Repeat ambiguous requests safely by upserting the same logical output keys.",
      "formula": "same inputs -> same embedding IDs",
      "topic": "Chunk-and-Embed as an Idempotent Batch Job",
      "highway": true
    },
    {
      "concept": "Token-aware batching",
      "intuition": "Pack requests against the exact tokenizer and model token limit rather than a fixed chunk count.",
      "formula": "sum input tokens + prefixes <= request token limit",
      "topic": "Chunk-and-Embed as an Idempotent Batch Job",
      "highway": false
    },
    {
      "concept": "Embedding readiness manifest",
      "intuition": "Reconcile expected, committed, failed, quarantined, and missing chunks before serving cutover.",
      "formula": "expected = succeeded + quarantined + missing",
      "topic": "Chunk-and-Embed as an Idempotent Batch Job",
      "highway": true
    },
    {
      "concept": "Vector-metadata binding",
      "intuition": "Attach every vector to chunk, document version, model, corpus, permissions, and processing state.",
      "formula": "vector -> embedding_id -> chunk_id -> source version",
      "topic": "Storing Vectors Beside Metadata",
      "highway": true
    },
    {
      "concept": "Typed vector namespace",
      "intuition": "Bind an index to one compatible model, dimension, normalization rule, and distance metric.",
      "formula": "namespace contract = dimension + metric + model version",
      "topic": "Storing Vectors Beside Metadata",
      "highway": true
    },
    {
      "concept": "Metadata synchronization",
      "intuition": "Propagate permission, deletion, and active-state changes to every serving copy and detect stale versions.",
      "formula": "source metadata version should equal index payload version",
      "topic": "Storing Vectors Beside Metadata",
      "highway": true
    },
    {
      "concept": "Pre-filtered ANN",
      "intuition": "Restrict search to eligible vectors so ineligible items do not consume the result budget.",
      "formula": "ANN(query | eligibility predicate)",
      "topic": "Pre-Filter vs Post-Filter in ANN Search",
      "highway": true
    },
    {
      "concept": "Post-filter oversampling",
      "intuition": "Retrieve more global neighbors, then remove ineligible items, accepting uncertain recall and extra work.",
      "formula": "rough candidates = k / eligible_fraction",
      "topic": "Pre-Filter vs Post-Filter in ANN Search",
      "highway": true
    },
    {
      "concept": "Filtered recall evaluation",
      "intuition": "Compare ANN results with exact search inside the eligible subset across real filter selectivity.",
      "formula": "filtered recall@k = ANN eligible hits / exact eligible top-k",
      "topic": "Pre-Filter vs Post-Filter in ANN Search",
      "highway": true
    },
    {
      "concept": "Authorization filter boundary",
      "intuition": "Remove ineligible candidates before any unauthorized content, metadata, reranking, or logging exposure.",
      "formula": "security predicate must fail closed inside trusted boundary",
      "topic": "Pre-Filter vs Post-Filter in ANN Search",
      "highway": true
    },
    {
      "concept": "Adaptive filtered search",
      "intuition": "Choose pre-filtering, oversampling, partitioned indexes, or exact search from subset size and policy.",
      "formula": "strategy = function(selectivity, security, index type, latency)",
      "topic": "Pre-Filter vs Post-Filter in ANN Search",
      "highway": false
    }
  ]
}
