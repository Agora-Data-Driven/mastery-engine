{
  "cards": [
    {
      "concept": "Bag-of-words vector",
      "intuition": "Give every vocabulary term an axis and represent a document by its nonzero term weights.",
      "formula": "d = [w(1,d), w(2,d), ..., w(V,d)]",
      "topic": "Documents as Bag-of-Words Vectors",
      "highway": true
    },
    {
      "concept": "Sparse document storage",
      "intuition": "A document activates few vocabulary axes, so store only nonzero term identifiers and weights.",
      "formula": "d = {(term_id, weight) | weight != 0}",
      "topic": "Documents as Bag-of-Words Vectors",
      "highway": true
    },
    {
      "concept": "Bag-of-words limitation",
      "intuition": "Term order and syntax disappear, so reordered or semantically opposite text can look similar.",
      "formula": "count(a b) = count(b a)",
      "topic": "Documents as Bag-of-Words Vectors",
      "highway": false
    },
    {
      "concept": "Dot product evidence",
      "intuition": "Only query terms also active in the document add lexical evidence.",
      "formula": "q dot d = sum_t q_t * d_t",
      "topic": "Cosine Similarity as a Ranking Function",
      "highway": true
    },
    {
      "concept": "Cosine similarity",
      "intuition": "Compare weighted vector direction while removing overall magnitude.",
      "formula": "cos(q,d) = (q dot d) / (||q|| * ||d||)",
      "topic": "Cosine Similarity as a Ranking Function",
      "highway": true
    },
    {
      "concept": "Cosine scale invariance",
      "intuition": "Multiplying every document coordinate by the same positive constant does not change its direction.",
      "formula": "cos(q, c*d) = cos(q,d), c > 0",
      "topic": "Cosine Similarity as a Ranking Function",
      "highway": false
    },
    {
      "concept": "Precomputed document norm",
      "intuition": "Store document normalization at indexing time and accumulate only matched-term evidence at query time.",
      "formula": "||d|| = sqrt(sum_t d_t^2)",
      "topic": "Document Length Normalization",
      "highway": true
    },
    {
      "concept": "Tunable norm penalty",
      "intuition": "An exponent interpolates between no length penalty and full cosine normalization.",
      "formula": "score = (q dot d) / ||d||^alpha",
      "topic": "Document Length Normalization",
      "highway": true
    },
    {
      "concept": "Pivoted length normalization",
      "intuition": "Penalize documents above collection-average length and modestly favor those below it.",
      "formula": "denom = (1-s) + s * length(d) / avg_length",
      "topic": "Document Length Normalization",
      "highway": false
    },
    {
      "concept": "Field-specific normalization",
      "intuition": "Normalize titles, bodies, and tags against their own typical lengths and semantics.",
      "formula": "score = sum_f boost_f * normalized_score_f",
      "topic": "Document Length Normalization",
      "highway": false
    }
  ]
}
